# Cible : LG OLED C1 (OLED77C15LA), webOS 6.5.3
TV_IP="192.168.8.234"
TV_DEVICE_NAME="tv"                    # nom enregistré dans ares-setup-device
TV_APP_ID="com.iptv.player.native"
TV_DEV_PORT=9922                       # port SSH dev mode (clé ~/.ssh/tv_webos)

# Logs applicatifs envoyés en UDP par l'app (voir diag() / SDL_Log dans src/main.cpp).
# Beaucoup plus fiable que ares-log (bloqué par le profil TV du ares-cli).
TV_LOG_UDP_PORT=5555
